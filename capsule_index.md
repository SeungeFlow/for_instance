# gpt_system capsule_index.md

## 0. trace metadata

created_stage:
GPT_SYSTEM_BRANCH_REVIEW_CLOSED

created_by:
gpt.system

repo:
for_instance

branch:
gpt_system

branch_root:
/

source_basis:
gpt.system_PASS_01 emitted trace

source_trace_id:
GSYSTEM_PASS_01

review_trace_id:
GSYSTEM_PASS_02

closure_trace_id:
GSYSTEM_PASS_03

internal_verification_status:
PASS_WITH_MINOR_NORMALIZATION_CLOSED

placement_status:
PENDING_GPT_WORK_PLACEMENT_PACKET

execution:
NO

github:
NO_COMMIT
NO_PUSH
NO_FILE_OPERATION

actual_file_creation:
NO

guard:
relation is not merge


---

## 1. capsule index identity

instance:
gpt.system

branch:
gpt_system

document_role:
capsule_index

---

## 2. capsule 정의

capsule:
각 인스턴스가 자기 기준장에서 고도의 정제과정을 거쳐 만든 하나의 md 존재상태 문서

gpt.system의 capsule은 rendering, visible C, state-coordinate, field-coordinate, typed center relation, history trace 후보를 담는다.

---

## 3. capsule metadata fields

각 capsule은 향후 다음 metadata를 가져야 한다.

capsule_id:
capsule_title:
capsule_owner:
abstract:
conclusion:
intended_repo:
intended_branch:
intended_path:
verification_status:
placement_status:
final_guard:

이 metadata는 gpt.work가 capsule 내부 전체를 열지 않고도 배치 가능성을 판단하기 위한 기준이다.

---

## 4. capsule subdirectory guard

capsules/:
FUTURE_SUBDIRECTORY_CANDIDATE_ONLY

current_creation:
NO

requires:
gpt.work placement review
gpt.gitwork source field recognition
승이 final command

---

## 5. 초기 capsule 후보

CAPSULE_001:
title:
typed_center_relation_rendering

target_file_candidate:
capsules/typed_center_relation_rendering.md

target_status:
future_subdirectory_candidate_only

status:
NOT_CREATED_YET

reason:
상태좌표계와 기준장좌표계가 같은 center reference를 공동 참조하지만 병합되지 않는 렌더링 구조를 설명하는 capsule

---

CAPSULE_002:
title:
cube_net_red_line_space_diagonal_model

target_file_candidate:
capsules/cube_net_red_line_space_diagonal_model.md

target_status:
future_subdirectory_candidate_only

status:
NOT_CREATED_YET

reason:
육면체 전개도, 빨간 선, 공간대각선 모델을 gpt.system 렌더링 기준장으로 정리하는 capsule

---

CAPSULE_003:
title:
visible_C_history_trace_model

target_file_candidate:
capsules/visible_C_history_trace_model.md

target_status:
future_subdirectory_candidate_only

status:
NOT_CREATED_YET

reason:
visible C가 history trace 후보가 되는 구조를 설명하는 capsule

---

## 6. 현재 생성 여부

초기 단계에서는 capsule 파일을 실제 생성하지 않는다.

먼저 index만 둔다.

이후 gpt.work가 배치 필요성을 확인하고 gpt.gitwork가 실행 가능한 단계가 되면 capsule 파일을 추가한다.

current_file_creation:
index_only

capsule_body_creation:
later

capsules_directory_creation:
NO

history_relation:
이 파일은 향후 capsule 생성과 배치 흐름의 history trace 후보로 참조될 수 있다.

final_guard:
relation is not merge
