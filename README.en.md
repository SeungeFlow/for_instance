# for_instance

This is the primary criterion-field repository for named GPT.LLM clone instances.

Each instance branch preserves its own role-assigned criterion-field.

The main branch records common process, initial history, reading order, and relation pointers.

Final guard:

relation is not merge.
