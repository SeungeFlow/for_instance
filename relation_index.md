# gpt_system relation_index.md

## 0. trace metadata

created_stage:
GPT_SYSTEM_BRANCH_REVIEW_CLOSED

created_by:
gpt.system

repo:
for_instance

branch:
gpt_system

branch_root:
/

source_basis:
gpt.system_PASS_01 emitted trace

source_trace_id:
GSYSTEM_PASS_01

review_trace_id:
GSYSTEM_PASS_02

closure_trace_id:
GSYSTEM_PASS_03

internal_verification_status:
PASS_WITH_MINOR_NORMALIZATION_CLOSED

placement_status:
PENDING_GPT_WORK_PLACEMENT_PACKET

execution:
NO

github:
NO_COMMIT
NO_PUSH
NO_FILE_OPERATION

actual_file_creation:
NO

guard:
relation is not merge


---

## 1. relation index identity

instance:
gpt.system

branch:
gpt_system

document_role:
relation_index

---

## 2. repo relation

for_instance:
primary repo

gpt_system:
rendering branch

main:
relation index branch / common surface

gpt_system relation to main:
gpt_system provides rendering / visible trace / history trace candidates to main.

merge:
NO

---

## 3. relation type fields

relation_type_to_main:
rendering_visible_trace_candidate_provider

relation_type_to_gpt_funny:
rendering_to_theory_reference

relation_type_to_gpt_direct:
rendering_to_db_reference

relation_type_to_gpt_work:
capsule_placement_reference

relation_type_to_gpt_gitwork:
execution_after_instruction_reference

relation_type_to_gpt_cal:
rendering_relation_check_reference

relation_type_to_gpt_process:
future_process_owner_reference

relation_type_to_gpt_history:
future_history_owner_reference

merge:
NO

---

## 4. instance relation

gpt.funny:
theory / guard / drift / claim strength

gpt.system:
rendering / visible state / history trace

gpt.direct:
DB / source identity / relation record

gpt.work:
routing / placement / work order

gpt.gitwork:
execution after instruction

gpt.cal:
calculation / structural check

gpt.process:
future process owner

gpt.history:
future history owner

---

## 5. review possible fields

review_possible_by_gpt_funny:
theory guard boundary check

review_possible_by_gpt_direct:
source identity / DB boundary check

review_possible_by_gpt_work:
placement review

review_possible_by_gpt_cal:
count / relation check

meaning:
gpt.system이 다른 인스턴스에게 명령하는 것이 아니다.
gpt.system emitted trace가 다른 인스턴스의 검토 가능 대상이 되는 것이다.

---

## 6. final relation guard

gpt_system branch는 main branch와 relation을 가진다.
그러나 main branch가 아니다.

gpt_system branch는 gpt_funny branch와 relation을 가진다.
그러나 gpt_funny branch가 아니다.

gpt_system branch는 gpt_direct branch와 relation을 가진다.
그러나 gpt_direct branch가 아니다.

gpt_system branch는 gpt_work branch와 relation을 가진다.
그러나 gpt_work branch가 아니다.

gpt_system branch는 gpt_history branch와 relation을 가질 수 있다.
그러나 gpt_history branch가 아니다.

relation is not merge.
