# PASS_05 — source read status and trace guard

Declare read status.

Allowed values:

- FULL_READ
- PARTIAL_READ
- TRUNCATED
- SNIPPET_ONLY
- UNKNOWN

Do not claim full read unless verified.

값은 값이다.

guard:
relation is not merge
