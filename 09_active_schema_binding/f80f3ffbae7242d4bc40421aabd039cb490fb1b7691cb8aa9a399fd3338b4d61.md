---
document_type: INTERNAL_FOR_INSTANCE_SEAT_CHANGE_MATRIX
semantic_name: for_instance File-by-File Change Matrix
schema_state: INTERNAL_DRAFT
source_authority: 승이
coordination_authority: gpt.logi@gpt.xyzt
created_at: 2026-07-21
github_mutation: false
---

# for_instance File-by-File Change Matrix

## 0. 변경원칙

```text
Existing Structure
→ Preserve

New Theory
→ Extension and precise correction

History
→ Append-only

Runtime Exact Objects
→ Hash Filename
```

## 1. Root

### `README.md`

추가:

- `for_instance = AI Instance Alignment Repository`
- 최초 읽기순서
- Stable Semantic Surface와 Exact Object 구분
- `gpt.xyzt.md` 진입주소
- 실행은 Common Directive + Individual Directive로 진행
- 공동지시문 직접실행 개념 종료
- 기존 Assignment·Registry·History 보존

## 2. `00_manifest/`

추가 또는 보완:

- 현재 Contract Version
- Internal Active_Schema Binding 후보
- Remote Readback 전 상태는 `UNVERIFIED_REMOTE`
- Hash Object naming contract

## 3. `01_seat_model/README.md`

추가:

- Seat ≠ Instance ≠ Occupation
- 한 자리·한 현재 상태값
- 상태 순차전이
- 0의 Type 구분
- Cell 최소성질
- 평면체 정의
- Function 단방향성
- `|←` 검수·검산 의미

## 4. `01_seat_model/INSTANCE_GENERATION_INDEX.md`

현재의 “7개 파일이 Cycle 객체를 생성한다” 표현을 교정한다.

새 역할:

```text
Seat Entry Index
+ Stable URL Registry
+ Read Order
```

명시:

```text
Seat 파일을 읽음
≠ Stage 실행

Stage 실행
=
필요 문서 선입력·인덱싱
+ READY_FOR_DIRECTIVE
+ 개별지시문 1개
```

## 5. Data Seats

### `gpt.x.md`, `gpt.y.md`, `gpt.z.md`

추가:

- Data Stage 전용 자리
- 주제·Source Boundary 선인덱싱
- 하나의 Data Output
- 특이점 화면표시
- 미래 Function·Result 지시를 현재 Data 상태에 넣지 않음

## 6. Function Seats

### `gpt.xy.md`

```text
Function.1.XY 직접입력
→ Data.X + Data.Y

직교 Z
→ XY 평면체 위치 Context
→ 직접입력 아님
```

Function.1과 Function.2를 서로 다른 Occupation으로 기록한다.

### `gpt.xz.md`

```text
Function.1.XZ 직접입력
→ Data.X + Data.Z

직교 Y
→ XZ 평면체 위치 Context
→ 직접입력 아님
```

### `gpt.yz.md`

```text
Function.1.YZ 직접입력
→ Data.Y + Data.Z

직교 X
→ YZ 평면체 위치 Context
→ 직접입력 아님
```

세 Seat 공통:

- 인덱싱 단계와 실행단계 분리
- Direct Input 외 보조 Dependency 금지
- Function.1 / Function.2 별도 지시문
- 하나의 현재 상태값
- 특이점 화면표시
- 다른 Seat Context를 의미입력으로 흡수 금지

## 7. Result Seat

### `gpt.xyz.md`

두 Occupation을 분리한다.

```text
RESULT_XYZ
→ Function.1 세 객체의 자리보존 정리

RESULT_DATA_XYZ
→ Result + Review 세 객체 + Correction Closure의 자리보존 정리
```

한 지시문에서 두 상태를 동시에 수행하지 않는다.

## 8. Overall Coordination Seat

### `gpt.xyzt.md`

새 역할:

- Fixed Seat / Fixed Role
- Minimal Grammar Pointer
- for_instance Address Relation
- Current Cycle Subject
- One Current Stage and State
- Indexed Object Set
- One Active Individual Directive Hash
- Anomaly Surface
- Gate Verdict
- Next Safe Action
- Append-only Event Pointer

지시문 전체본문의 무제한 내장 금지.

## 9. `03_relation_interface/`

추가할 계약후보:

- Stable Semantic Surface ↔ Exact Hash Object Relation
- Repository / Branch / Path / Web / Raw / Role / Read Order
- for_instance ↔ Relation Track DB 경계
- Address가 Source URL이 아니라 Operation Position임을 명시

## 10. `06_operation/`

Hash-named Exact Object로 배치할 대상:

- Minimal Common Directive
- Indexing Protocol
- Individual Directive Template
- Anomaly Surface Contract
- gpt.xyzt Adaptive Gate Contract
- 11-Stage Protocol
- Correction and Supersession Contract

## 11. `07_tests/`

필수 테스트:

```text
TEST_ONE_POSITION_ONE_STATE
TEST_DOCUMENT_BEFORE_DIRECTIVE
TEST_INDEX_READY_GATE
TEST_DIRECT_INPUT_ONLY
TEST_CONTEXT_NOT_DEPENDENCY
TEST_FUNCTION_1_FUNCTION_2_SEPARATION
TEST_RESULT_NOT_RESULT_DATA
TEST_RESULT_DATA_NOT_TRACK_DB
TEST_ADAPTIVE_NEXT_DIRECTIVE
TEST_BLOCKING_ANOMALY_STOPS_NEXT_STAGE
```

## 12. `08_history/`

- 기존 Event 수정·삭제 금지
- 새 계약은 Directed Difference Event로 추가
- 공동지시문 과거 실행은 Historical/Superseded로 보존
- 0004 Attempt는 비교 Evidence로 보존

## 13. `09_active_schema_binding/`

배치할 것:

```yaml
active_schema_binding:
  package_sha256:
  manifest_sha256:
  semantic_scope:
  internal_or_published_state:
  deployment_commit:
  deployment_tree:
  readback_state:
```

배치하지 않을 것:

```text
Active_Schema Package 전체 복사본
모든 내부 이론문서 중복
Runtime Result.Data
```
