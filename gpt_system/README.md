# main/gpt_system pointer

This directory is a main-branch pointer to branch.gpt_system.

It is not the gpt_system branch itself.

branch.gpt_system:
full local rendering / visible state field

main/gpt_system/:
accepted summary / branch pointer / relation index only

relation is not merge.
